version: 2

models:
  - name: stg_payments
    columns:
      - name: customer_id
        description: Putting this on for more description in for practice.
        tests:
          - unique
          - not_null
      - name: first_name
        description: First name of the customer
        tests:
          - not_null

